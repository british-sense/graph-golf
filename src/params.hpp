#pragma once
#include <random>

// general
static int degree;

// general graph
static int node;

// grid graph
static int height;
static int width;
static int length;

// others
static std::mt19937 mt;